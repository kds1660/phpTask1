{% block content %}
    {{ render(controller(
    'AppBundle:Block:studioFilms', {'studio':studio}
    )) }}

    {{ render(controller(
    'AppBundle:Block:studioActors', {'studio':studio}
    )) }}
{% endblock %}